-- lua globals, properly declared because they are used across a variety of composer scences.

_G.phoneNumber = "empty" --is a string instead of an integer because androids SMS function wants a number in string format.
_G.userName = "empty"
_G.verifyCode = 0